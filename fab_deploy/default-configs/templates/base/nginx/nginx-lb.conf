{% extends "base/nginx/nginx.conf" %}

{% block upstreams %}
    # Add application servers
    upstream app_servers {
        ## Start App Servers ##
        server   x.x.x.x:8000 max_fails= {{ upsteam_max_fails }}  fail_timeout= {{ upstream_timeout }}s;
        ## End App Servers ##
    }
{% endblock %}
